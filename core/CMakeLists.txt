# The "core" folder holds all of the C++ robotics logic: sensors,
# localization, mapping, planning, control, perception, missions, and
# shared/common code. We add each piece as we build it.

add_subdirectory(common/math)
